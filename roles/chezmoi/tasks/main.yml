---
- name: Initialize chezmoi repository from repo
  ansible.builtin.command:
    cmd: chezmoi init {{ chezmoi_https_repo }}
  args:
    creates: ~/.local/share/chezmoi

- name: Apply chezmoi dotfiles
  ansible.builtin.command:
    cmd: chezmoi apply